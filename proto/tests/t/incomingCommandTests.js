/**
 * Created by steb on 20/06/2016.
 */
define(['./_init', 'core/utils'], function (_init, utils) {

    describe('cs: incomingCommandTarget', function () {
        var cs;

        _init(function (_cs) {
            cs = _cs;
        });

        xit('приоритетнее смены основания');

        xit('работает в таких случаях: calcState[wait|wait2]');

        describe('сценарии', function () {
            it('a+b=', function(){
                
            });
            
            it('a+-b=', function(){
                
            });
            
            it('a=', function () {
                
            });
        });
        
        it('фиксация переполнения', function () {
            
        });
    });
});